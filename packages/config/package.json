{
  "name": "@datarango/config",
  "version": "0.0.0",
  "private": true,
  "_comment": "No `exports` field on purpose. Adding one restricts subpath resolution to whatever it lists, which immediately broke every tsconfig that extends @datarango/config/tsconfig/*.json. Both the tsconfigs and the eslint presets are consumed by literal path, so free resolution is what this package wants.",
  "dependencies": {
    "@eslint/js": "^9.0.0",
    "@next/eslint-plugin-next": "^16.0.0",
    "eslint": "^9.0.0",
    "eslint-plugin-react-hooks": "^6.0.0",
    "typescript-eslint": "^8.0.0"
  }
}
