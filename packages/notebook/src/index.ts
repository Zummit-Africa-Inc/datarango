/**
 * @datarango/notebook — the Jupyter notebook client (FRONTEND-HANDOFF §6).
 *
 * Phase F3 (backend Phase 3) builds: typed kernel protocol client over the
 * gateway WS proxy, CodeMirror 6 cells, virtualized cell list, output rendering
 * (sandboxed iframes for HTML), useNotebookStore/useKernelStore, exercise mode.
 * Mounted by `web` (learn + notebooks) and `studio` (exercise authoring).
 */
export {};
