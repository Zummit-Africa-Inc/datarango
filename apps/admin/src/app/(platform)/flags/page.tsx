"use client";

import { PageLayout } from "@datarango/ui";

const Page = () => {
  return (
    <PageLayout title="Feature Flags" subtitle="Manage feature flags for the platform.">
      <div>Page</div>
    </PageLayout>
  );
};

export default Page;
